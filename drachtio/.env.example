# Drachtio server connection
DRACHTIO_HOST=127.0.0.1
DRACHTIO_PORT=9022
DRACHTIO_SECRET=cymru

# SIP registration (e.g., with FreeSWITCH or Kyivstar)
SIP_DOMAIN=192.168.50.100
SIP_USERNAME=drachtio-echo
SIP_PASSWORD=password
SIP_PORT=5060

# RTP port range for echo service
RTP_PORT_MIN=10000
RTP_PORT_MAX=10100

# Local IP for RTP (change to your machine's IP)
LOCAL_IP=192.168.50.2